# RISEFALL live bot — Railway "worker" service
# CPU-only torch wheel (Railway has no GPU; the default PyPI index otherwise
# pulls a much larger CUDA-bundled build). Order matters: --extra-index-url
# must come before the package that should use it.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.24,<2.3
requests>=2.31
websockets>=12.0
scipy>=1.11
statsmodels>=0.14
hmmlearn>=0.3
arch>=6.2
torch>=2.2,<3.0
